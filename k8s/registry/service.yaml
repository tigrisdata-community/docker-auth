apiVersion: v1
kind: Service
metadata:
  name: self-hosted-docker-registry
spec:
  selector:
    app: self-hosted-docker-registry
  ports:
  - port: 80
    targetPort: 5000
    name: http
    protocol: TCP
